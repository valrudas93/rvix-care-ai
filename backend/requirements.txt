# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# Validation / settings
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.3.0
python-multipart==0.0.19

# Image handling (Keras / TensorFlow pipeline)
tensorflow==2.17.0          # includes Keras 3 — use tensorflow-cpu for CPU-only servers
Pillow==11.0.0
numpy==1.26.4

# LLM providers (install only what you need)
openai==1.57.0              # for LLM_PROVIDER=openai
anthropic>=0.40.0           # for LLM_PROVIDER=anthropic
# mistralai==0.4.2          # for LLM_PROVIDER=mistral
# llama-cpp-python==0.3.2   # for LLM_PROVIDER=llama  (requires cmake)

# Dev / testing
python-dotenv==1.0.1
httpx==0.27.2               # for FastAPI TestClient
pytest==8.3.4
